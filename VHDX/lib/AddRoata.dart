import 'package:flutter/material.dart';

class AddRoata extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Card(child:Container(
      color: Colors.black,
      padding: EdgeInsets.all(10),
      child: Text("Hello you!")),
      
    );
  }
}